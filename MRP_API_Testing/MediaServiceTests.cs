using MRP_API.Interfaces;
using MRP_API.Models;
using MRP_API.Services;
using NUnit.Framework;

namespace MRP_API.Tests
{
    [TestFixture]
    public class MediaServiceTests
    {
        /*[SetUp]
        public void Setup()
        {
            MediaService.Clear();
        }

        [Test]
        public void CreateMedia_AssignsId()
        {
            var media = new MediaEntry
            {
                Title = "Interstellar",
                Type = "movie",
                ReleaseYear = "2014",
                Genre = "sci-fi",
                AgeRestriction = "PG-13"
            };

            var created = MediaService.CreateMedia(media, "Test");

            Assert.That(created.Id, Is.GreaterThan(0));
        }*/
    }
}
